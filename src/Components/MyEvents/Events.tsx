import { Grid } from '@mui/material';
import React from 'react';

const Evenets = () => {
  return (
    <Grid style={{ height: '200vh', backgroundColor: 'green' }}>
      <Grid>Evenets</Grid>
    </Grid>
  );
};

export default Evenets;

import { Grid } from '@mui/material';
import React from 'react';

const AddNewPoster = () => {
  return (
    <Grid style={{ height: '200vh', backgroundColor: 'blue' }}>
      <Grid>AddNewPoster</Grid>
    </Grid>
  );
};

export default AddNewPoster;
